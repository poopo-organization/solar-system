pipeline {
    agent any

    stages {
        stage('VM Node Version') {
            steps {
                // Execute shell commands
                sh 'node -v'
                sh 'npm -v'
            }
        }
    }
}